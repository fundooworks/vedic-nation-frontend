version: 1
frontend:
  preBuild:
    commands:
      - echo "prebuild"
  build:
    commands:
      - echo "Build"


  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  
  cache:
    paths:
      - public/**/*
